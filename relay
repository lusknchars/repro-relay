#!/usr/bin/env python3
"""Repository-local entry point; no installation or global configuration needed."""
import pathlib
import runpy
runpy.run_path(str(pathlib.Path(__file__).resolve().parent / 'integrations/relay-terminal/cli.py'), run_name='__main__')
