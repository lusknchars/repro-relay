# Current milestone

Hermes and Plow artwork, September 14: the default web/desktop client now uses the supplied logos beside Hermes identities in Work, Team, Usage and runtime setup, and beside Hermes/Plow connection entries and Plow follow-ups. The artwork is copied unchanged, with preserved proportions and adjacent text labels. No runtime, connection or sample-data behavior changed. Image loading, connection details, runtime setup, light/dark display and phone bounds were checked in the browser. `make check` passed, including all 33 browser tests. The desktop bundle rebuilt and reopened.

Brand mark, September 14: the supplied black/white PNGs and SVG are stored unchanged. The default sidebar replaces RR with the black mark in light mode and the white mark in dark mode, retaining its existing spacing. The browser favicon adapts to system appearance; the rebuilt desktop icon places the same geometry on a light tile. The layout and sample content are otherwise unchanged. Browser inspection passed for both decoded theme variants, the collapsed accessible name and favicon delivery, with no page errors. `make check` passed, including all 33 browser tests. The desktop bundle rebuilt and reopened; its actual window was visually checked.

Exact ZIP frontend, September 14: the user rejected the prior adaptation. The default client now uses `web/reptest` with the supplied screens, CSS, theme, sample data and lockfile unchanged. The Tauri bundle embeds that frontend. Its sample connections, agent activity and costs are not live. The previous backend client remains accessible with `dev:legacy`, and its regression suites explicitly target it. See [the integration record](REPTEST-ADAPTATION.md). Native repository commands remain implemented but are not yet invoked from the supplied screens.

Validation: all 22 original source/configuration files matched the supplied archive byte for byte. `make check` passed, including the new Reptest navigation/appearance/mobile test and 32 retained-client browser tests, plus Python and backend checks. Native Clippy and the macOS debug build passed. The actual app was opened and its window captured showing the supplied interface. Windows/Edge CI now includes the supplied frontend but has not been run here. These checks do not establish live backend behavior in the prototype.


Reptest frontend adaptation, September 14: the supplied prototype now informs the shared web/desktop shell, semantic colors, compact sidebar and work views. Five primary destinations organize Work, Team, Knowledge, Usage and Settings; previous case and investigation URLs remain supported. The work list appears before aggregate activity, and completed onboarding becomes a compact replay bar. [The adaptation map](REPTEST-ADAPTATION.md) distinguishes working features from the prototype's sample data.

Usage now has a dedicated case/attempt selector backed by recorded Hermes telemetry and the existing graphs. Local validation remains excluded and missing cost stays unknown. Team uses real account and invitation controls; Team and Usage are valid invitation destinations. The appearance panel persists validated device preferences for accent, radius, density, text size, content width, sidebar variant/collapse and effects. Manual reduced motion stops transitions and hides animated backgrounds, while the system preference remains respected. Mobile has five bottom destinations. The Tools library and saved Pi memory preference remain available in Settings.

Validation: backend and Python suites, formatting, Clippy and production builds passed. All 25 existing workflow tests passed; focused tests additionally cover appearance persistence, keyboard focus, 320px navigation, case-bound usage, missing costs and refresh failure. The revised onboarding test passed. Guest navigation passed after the static build incorporated explicit accessible labels. Five account tests cover the expanded invitation allowlist. Across the final runs, 32 browser tests passed, including the runner and Chromium compatibility suites. The final side-by-side Usage charts passed the focused browser checks. The macOS debug bundle rebuilt, and live light/dark screens were visually inspected. Prototype chat, direct messages, inferred spending and invented active-agent states were not imported. Native OS interactions and Windows/Edge remain unverified.

Tools library, September 14: Connections now offers eight searchable, categorized entries for Relay evidence, Mem0, Pi, Kimi, Hermes, context monitoring, Plow/Latch and WebMCP. Each explains its purpose, access and cost source, with a working setup or navigation action. Existing Pi/Codex and browser recipes remain in an expandable section. The supplied discovery screenshots informed this arrangement; PaceUI remains the only dashboard template.

Mem0 can be enabled for new Pi sessions through a versioned, workspace-scoped preference. The terminal launcher reads it unless an explicit `--memory off` or `--memory mem0` overrides it. Guest workspaces and team viewers cannot change the preference. Conflicting or uncertain writes require refreshing before another change. Account setup stays in private CLI prompts, and saved selection is not presented as verified authentication. No model, provider signup, note upload or message delivery is triggered by the switch.

Validation: backend formatting, Clippy and PostgreSQL suites passed, including selection persistence, conflicts, credential-field rejection, workspace isolation and viewer permissions. Python integration suites passed. After correcting an ambiguous browser-test selector, all 25 workflow tests, two guest tests, one runner test and two Chromium compatibility tests passed. The final library layout also passed focused browser tests. Live Connections and Mem0 setup were inspected with no page errors, and the shared macOS debug bundle rebuilt. Native OS interaction and Windows/Edge were not exercised. Mem0/Moonshot billing remains external; only existing Hermes telemetry appears in Relay charts. The designer handoff now includes the tool-library requirement and the remaining marketplace/authentication work.

Designer handoff, September 14: [the UI/UX brief](DESIGNER-HANDOFF.md) defines a proposed unified Work workspace, connection and billing clarity, evidence-based decisions, context inspection, usage graphs, desktop tools and phone continuity. It separates current implementation from required backend work and future concepts. This is a design deliverable; no UI or runtime behavior changed.

Optional Mem0 memory, September 14: one agent-mode account now provides separate Hermes and Pi working-note scopes. Notes are short, immutable, unverified and journaled locally before sending. Shared reviewed observations still come from Relay's current database records. Retrieval validates scope and exact note contents; forgotten notes stay hidden, and uncertain writes are not automatically repeated. Pi enables the two tools with `--memory mem0`; the dedicated Hermes profile adds them through `runtime.py enable-memory`. Provider authentication remains separate. See [setup and ownership](../integrations/mem0-memory/README.md).

Live Mem0 signup, connection, short-note storage and separate-agent recall passed. Installed Hermes discovered both MCP tools without a model call. Mem0 billing is not yet included in Relay's usage charts, and token savings have not been measured. The account is unclaimed; the owner should run `~/.local/bin/mem0 init --email YOUR_EMAIL` to complete ownership. Hermes still needs its own provider sign-in before a live investigation.

Validation: `make check` passed, including 28 Chromium tests. Nine focused memory tests passed after the final project-binding check; nine Pi tests include real extension loading with memory off and on, fixture evidence reads and zero model tokens. The Pi extension also type-checked against the installed API. Live model-directed recall/write and cost reduction remain unverified.

Pi sign-in clarification, September 14: the Relay launcher now accepts `--profile personal` to use an existing login in `~/.pi/agent` without copying credentials. Its explicit evidence tool allowlist and Relay session directory remain in effect. `pi doctor --provider NAME` checks local credential readiness without refreshing or returning credentials; an explicitly requested provider that is not ready causes a nonzero exit. Start requires an interactive terminal and explains why `/login` and plain `pi start` are not Relay shell commands. Hermes authentication remains independent.

The live personal Anthropic credential check reported ready; the dedicated Relay profile had no Anthropic login, and the live Hermes investigator was unavailable. A prompt-free Pi startup selected the saved Anthropic model. No model call or billing/entitlement validation was performed. Eight focused tests passed for selected-profile preservation, safe readiness output, non-interactive guidance and the existing real Pi evidence flow. `make check` passed, including 28 Chromium tests.

Pi terminal integration, September 14: Pi 0.85.1 is installed locally through the inspected official installer. `./relay pi doctor` checks the executable and local evidence API separately from provider access. `./relay pi start` loads a dedicated ignored profile, the Relay extension and an explicit allowlist of the three shared evidence tools. `/relay` opens the latest context audit without a model call; `/relay-review` supplies that audit and its review task to the selected model. Empty history does not start a model. Connection setup in web and desktop now offers Pi / Codex choices with compact steps and expandable details.

Pi follows the existing loopback, validation, pagination and read-only evidence contract. It preserves source revision and review links, uses bounded process execution, and does not enable repairs, approvals, tests, memory writes or external delivery. Pi authentication, session history and usage remain separate from Hermes; no provider sign-in, paid Pi review or Hermes runtime activation is claimed. See [Pi setup and scope](../integrations/pi-harness/README.md).

Validation: `make check` passed, including 28 Chromium tests, and the desktop bundle rebuilt. Four Python tests cover an actual Pi RPC extension load, exact active-tool inspection, fixture audit retrieval and zero model tokens. The extension also passed type-checking against the installed Pi API. A separate live read retrieved the current audit from the running workspace without writes or model calls. The live Connections page was checked at desktop and 320px widths with no page errors or horizontal overflow. A final focused browser check covers selection, copied commands and distinct usage explanations after the setup card was shortened. Provider execution, native Terminal launch into Pi and Windows/Edge remain unverified.

Native developer workspace, September 14: the desktop now has a compact repository bar with native folder selection, remembered checkout, tracked Git changes, existing worktrees and a macOS Terminal handoff. Repository tools are available even when the case service is offline. Cmd/Ctrl+J toggles the panel; Cmd/Ctrl+Shift+O opens the folder picker. Short connection indicators distinguish the local workspace service from Hermes availability. Expanding the panel reserves space so underlying content remains reachable, with reduced-motion support.

The user-supplied Kiro Crew recording informed the stable navigation, concise state labels and expandable details. See [the developer workspace and comparison](DESKTOP-DEVELOPER-WORKSPACE.md). The bridge accepts no arbitrary command or repository path. Native Git reads are bounded, disable configured content filters and filesystem monitoring, and do not change case targets. This is a native inspection and terminal-handoff increment; embedded PTY, source editing, service lifecycle management and autonomous repair execution are not added.

Validation: `make check` passed, including 27 Chromium tests. Two native Rust tests exercised real temporary repositories, unborn HEAD, tracked changes, detached worktrees, filter suppression, invalid repositories and output bounds. Native Clippy passed and the desktop bundle rebuilt. Browser fixtures cover the native command contract, offline usability, worktree inspection, terminal handoff, keyboard collapse and minimum desktop width; they do not prove OS picker or terminal-window interaction. Windows/Edge, native UI automation and screen-reader operation remain unverified.

Profile navigation and overview simplification, September 14: the top-right avatar now opens a keyboard-operable account menu with the saved profile identity and role. Profile, owner team invitations, and password settings open separate sections of the existing dialog. The menu also navigates to Overview and Connections, changes the persisted theme, and signs out through the existing backend. Desktop account settings continue in the browser. The Investigation desk status cards and four summary statistic cards were removed from the overview at the user's request; case records, inbox status filters, investigation activity and Hermes controls remain.

Validation: `make check` passed, including 26 Chromium tests. The desktop bundle rebuilt. The live signed-out menu was checked at desktop and 320px widths without account writes, horizontal overflow or JavaScript errors. Browser fixtures cover identity refresh after profile edits, menu-to-dialog focus, direct settings navigation, theme synchronization, sign-out and mobile bounds. Authentication behavior is unchanged. Windows/Edge, screen-reader and native menu interaction remain unverified.

Accounts and team return links, September 14: the local installation now offers Account & team in the top bar. The first owner creates a username/password account without changing existing cases. Profiles retain a display name and description. Single-use invitations return a teammate to the selected case or saved audit and grant viewer access to the shared installation. The owner can revoke invitations or remove access. Authenticated team mode rejects viewer mutations on the server and requires an owner to exist before startup. Local mode remains trusted; guest beta workspace isolation is unchanged. See [account setup and sharing](ACCOUNTS-AND-TEAM.md).

Passwords use Argon2id; session and invitation tokens are stored as hashes. HTTPS cookies, Origin checks, rate limits, password-change session revocation and transactional access audit records are implemented. The desktop account control opens the browser; remote team access is currently web-based. No personal account, shared HTTPS deployment, email delivery, password recovery, MFA or owner transfer was created. Hermes still needs its separate provider sign-in; no provider execution or token spend is claimed.

Validation: `make check` passed, including 25 Chromium tests and five new PostgreSQL account tests. Final focused validation covers transactional audit records and the final phone dialog layout. The actual local owner setup was inspected at desktop and 320px widths without creating an account or changing cases. The desktop bundle rebuilt. Browser account flows use fixtures; backend authorization tests use isolated PostgreSQL databases. Cross-device HTTPS sharing, native account-link interaction, Windows/Edge and screen-reader use remain unverified.

Hermes assessment setup and test triage, September 14: a dedicated Hermes 0.21.2 installation and private assessment home are prepared locally. The profile exposes only two captured-evidence MCP tools, verified through Hermes's actual discovery and selection code. Source snapshots, browser accessibility trees and test receipts are immutable within each tool process. Memory, tool-search indirection and MCP model sampling are disabled. The official separate provider sign-in is still pending, so no Hermes assessment or token spend is claimed. See [setup and operation](../integrations/hermes-assessment/README.md).

The local application contains a new Platform usability and test coverage assessment case, `RR-7615b63732ba4a78a9fe71ac20748c8b`. Its saved local validation has 30 actual layout, page-error and read-only-navigation checks across 1440px and 320px views, with four explicit unrun checks: Hermes assessment, Windows/Edge, screen-reader operation and physical-phone interaction. The packet records base commit and dirty working-tree scope. Repeating publication retained the same case/run/receipts. These are bounded navigation checks, not full workflow or accessibility certification.

Test triage opens on checks needing attention and groups explicit structured receipts by outcome with source links, timestamps and capture provenance. Counts cover loaded history, and late-arriving starts cannot override newer producer outcomes. Evidence refresh follows the runtime checked timestamp even when its status/version is unchanged, preserving the selected filter, open log and focus. Local-validation provenance is now accepted by the evidence API. Native Hermes's terminal `interrupted` status records a failed run, preserves partial output/usage and releases the workspace slot without retrying.

Validation: `make check` passed, including 23 Chromium tests, with additional focused tests for local-validation persistence, unchanged-version evidence refresh and the private runtime/MCP setup. Actual tool discovery resolved exactly the two assessment tools without provider calls. The local page displayed all 34 recorded checks with no mutation requests. The desktop bundle rebuilt. Provider execution, streamed Hermes tool events, native repair extensions, Windows/Edge, physical-device and screen-reader sessions remain unverified.

Current work and direct result review, September 14: the investigation page now distinguishes current Hermes activity from saved history. Runtime availability refreshes with run state. The activity panel reports disconnected, idle, active investigation, approved repair, protected verification, failed refresh, and unconfirmed-start states. Selecting an old result while work continues exposes View active work. The actual local runtime remains unconfigured; its saved local validation records are explicitly labeled and no longer display empty Hermes usage charts.

Saved results now offer Accept proposal, Needs another check, and Dismiss proposal without a diagnosis form. These decisions reference the existing run/version through the canonical idempotent review API. Optional feedback and reviewer naming remain available in a disclosure; a supplied local name is remembered. Otherwise the visible reviewer label is Local workspace user. A request for another check includes the saved report, proposal, and review in the existing follow-up preview; starting the run remains explicit. No human observation, diagnosis, verified fix, code-edit permission, or runtime connection is fabricated by these controls.

Validation: `make check` passed formatting, Clippy, 77 PostgreSQL integration tests, two usage unit tests, 49 Python tests, the production build, and 22 Chromium tests. Final focused checks also passed direct decisions without form entry, unchanged-request retry after an uncertain response, persisted decisions, active repair versus historical record selection, connection loss, uncertain starts, and local records without Hermes charts. The runner fixture exercises a real review API write and confirms that case revision, observations, and run count are unchanged. The local page showed the actual disconnected runtime and existing saved validation without any write requests. Desktop rebuilt. Live Hermes execution, native desktop interaction, screen-reader sessions, and Windows/Edge remain unverified.

Investigation usability, September 14: the shared PaceUI web/desktop workspace now opens status-based triage queues from an Investigation desk. Each case shows a plain-language next step and opens its exact investigation directly. Navigation destinations explain their purpose. Phone case rows retain visible status and investigation actions without horizontal scrolling; redundant overview navigation cards were removed.

Investigation pages now explain the next action from actual saved state and provide keyboard-operable jumps to findings, test/performance evidence, usage, and run controls. Focus stays clear of the sticky header. Findings and reviews precede accounting, while the raw context packet starts collapsed. No provider-selection flow, task-direction form, automatic execution, or new performance result was added. [The Kiro benchmark](research/kiro-usability-benchmark.md) records the design decisions and remaining execution work.

Validation passed formatting, Clippy, 77 PostgreSQL integration tests, two usage unit tests, 49 Python tests, the production frontend build, and 21 Chromium tests. The runner test now opens the collapsed context disclosure before comparing its rendered contents with the API. New coverage checks filtered triage, selected-case persistence, keyboard focus below the header, 44-pixel phone actions, light/dark 320-pixel layouts, disconnected controls, and navigation with zero mutations. Real local case navigation was inspected at desktop and phone sizes without JavaScript errors or writes. The macOS desktop bundle rebuilt. VoiceOver/NVDA, physical devices, Windows/Edge, and a full accessibility certification were not exercised in this pass.

Repair execution ledger, September 14: `./relay ledger` adds deterministic local state to prepared, approved Git worktrees. Inform rechecks observed file hashes and summarizes fresh/stale observations, changes, command proposals, reported outcomes, and read reuse counts. Govern permits reference-only reuse for an identical file/range only when the caller declares the original receipt visible in the active agent context. Repeated test and other command proposals receive a nudge, never a cached result or execution permission. External edits and failed reads invalidate reuse; command outcomes remain caller-reported, immutable, and separate from backend repair verification.

The private SQLite ledger lives outside the candidate checkout, preserves paginated action history across restarts, and is bound to the original repair contract and worktree. Dirty candidate files and descendant commits remain inspectable. No additional model calls occur. POSIX no-follow file access is required; native Windows support is not implemented for this module. This is an adapter foundation: Hermes hooks are not connected, and it does not automatically intercept actions, execute repairs, establish a process sandbox, or demonstrate token savings. See [terminal workflow and adapter contract](../integrations/relay-terminal/README.md#execution-ledger-inform-govern-observe) and [the source-paper review](research/execution-ledger-paper.md).

Validation: `make check` passed formatting, Clippy, 77 PostgreSQL integration tests, two usage unit tests, the production frontend build, and 20 Chromium tests. The final focused suite passed all 18 terminal tests, including ten ledger tests for context-visible reuse, external edits/deletion/restoration, edits during reads, persistence, contract isolation, candidate commits, revocation, symlinks, size limits, capacity rollback, unsuppressed tests, outcome history, and actual CLI parsing. Other Python suites passed their existing 31 tests. Tests use temporary files and real temporary Git worktrees; no live correction was performed. The actual local doctor check still reports an unconfigured Hermes runtime. Web/desktop UI code was unchanged.

Local Git worktree and terminal controls, September 14: the repository now has an executable `./relay` entry point using Python and Git. It checks the real local runtime, lists and inspects cases, freezes an investigation preview, follows durable runs, requests stops, and operates exact-version repair approval/dispatch. A reusable setup file supplies repository scope and original test commands. Plan creation observes the real root and HEAD, refuses uncommitted changes, and delegates source eligibility to the existing backend.

`relay worktree` prepares a detached checkout for an approved plan, outside the source checkout, with repository/base identity checks and changed-checkout protection. It does not launch an agent, mount the checkout into a remote runtime, merge, or push. Git worktrees are not process sandboxes. Repair dispatch still requires the configured Hermes extension's isolated repair and protected verification capabilities; the current real local runtime remains unconfigured. There was no live code correction. Connections exposes the local doctor command beside the existing read-only MCP setup. See [terminal usage and execution limits](../integrations/relay-terminal/README.md).

Validation: `make check` passed formatting, Clippy, 77 PostgreSQL integration tests, two usage unit tests, 39 Python tests, the production build, and 20 Chromium tests. Eight terminal tests cover transport restrictions, write identities, plan/stage gates, case pagination, watcher behavior, real temporary Git worktrees, symlinks, and changed-file preservation. The final CLI command names passed the focused suite. The CLI also read the real local health, runner status, case list and saved investigation history. The macOS desktop bundle rebuilt. Only PostgreSQL is running in Docker; no Hermes CLI was found. Local repair generation, protected execution, and end-to-end candidate verification remain unexercised and require a compatible execution adapter.

Guided workspace and inline usage charts, September 14: Overview now invites users into a seven-page tour, with explanations beside the actual pages, Back/Next controls, Skip, and Replay. It preserves active/finished state across reloads separately for local and guest mode. The tour never invokes work mutations. Keyboard focus follows each step, and its heading stays clear of the sticky top bar.

Agent controls now shows Hermes usage charts in the main investigation column. A Tokens / Cost · USD switch controls a time-based reporting graph and a per-investigation comparison. The graphs read the existing run poll, preserve missing readings and downward revisions, exclude imported local validation, and offer exact values under Chart data. Saved totals without retained metric history appear as a single total rather than an invented timeline. Accounting details and JSON export remain optional expandable sections. No new model call, pricing estimate, or billing API was introduced.

Validation: formatting, Clippy, 77 PostgreSQL tests, two usage unit tests, 31 Python tests, production build, and 20 Chromium tests passed. The runner suite was rerun in a fresh isolated test database after an earlier aborted fixture retained the active slot; its full lifecycle and optional export passed. Tests cover all seven tour pages, reload, skip/replay, focus, mobile width, chart polling, missing/zero values, revised totals, and totals without retained history. The real local tour was inspected in light/dark and mobile layouts; populated graphs use labeled UI fixtures. The macOS desktop bundle rebuilt. Live Hermes billing, Windows/Edge, and native interaction were not exercised.

Plow connection action, September 14: the Connections card now opens a Connect Plow Latch dialog. macOS desktop can launch the installed app using its fixed bundle ID; web users can copy the equivalent Terminal command. The dialog provides the existing bridge setup guide, including line authorization and the read-only doctor check, plus the official Plow setup link. Guest workspaces direct setup to the local Mac. The guide downloads in browsers and uses the native save dialog in desktop. Opening Latch never changes the connection badge, starts Hermes, or sends a message. Line provisioning and bridge verification remain separate local setup steps.

Validation: `make check` passed Rust formatting, Clippy, 77 PostgreSQL integration tests, two usage unit tests, 31 Python tests, the production build, and all browser suites. Two new browser tests cover opening setup, exact command copying, clipboard failure, guide contents, guest restriction, Escape/focus restoration, and 320-pixel layout. The real local dialog and guide download were also exercised; desktop and mobile screenshots were inspected. The fixed Mac launch command succeeded, and the macOS desktop bundle rebuilt. Native button/save-dialog interaction and Windows/Edge were not exercised. No line credential, phone import, owner delivery, or live Hermes run was created.

Hermes cost inspection, September 14: Agent controls now shows selected-run token breakdowns, optional reported dollar cost, model/provider labels, per-field observation times, a bounded changed-report journal, case spending coverage, source identity, and JSON export. The backend preserves earlier readings when a partial update omits them, rejects invalid counters, records downward revisions, and never sums cumulative polling reports. The last 100 changed reports are retained with an omitted count. Older runs remain readable with unknown reporting timestamps. See [the accounting contract and live setup](HERMES-COST-INSPECTION.md).

Validation: `make check` passed 77 PostgreSQL integration tests, two usage-accounting unit tests, 31 Python tests, and 15 Chromium tests, plus formatting, Clippy, and production build. An additional focused browser test passed zero/missing-cost distinction, case coverage, local-validation exclusion, and 320-pixel layout. The protocol fixture verifies JSON export tied to run ID and context hash. Desktop rebuilt with JSON save support. The local API was restarted with the new accounting code. The local Hermes probe remains unconfigured; no paid run or live usage was generated. Native save-dialog interaction, Windows/Edge, per-tool cost attribution, provider bill reconciliation, and hard spending caps remain unverified or unimplemented.

Approval and navigation buttons, September 14: the user's examples now supply two reusable controls inside the existing PaceUI layout. Candidate approval, investigation review, and reviewed-memory publication use animated loading, success, and error icons driven by actual API outcomes. Pending saves block repeat clicks; confirmed candidate approvals remain disabled, and editing review feedback permits a new review. Unknown review outcomes keep their existing idempotent retry path. Memory publication retains the existing dialog-close and success-notice behavior after confirmation. No simulated results or automatic approval retries were introduced.

Workspace-guide navigation and dashboard Open workspace actions use the dot-to-arrow pill button, with hover and keyboard-focus expansion, fixed width, theme colors, and a visible arrow on touch devices. Both controls support reduced motion. Motion 13.2.0 was added for the requested presence transitions; Lucide supplies the existing icons. The production initial JavaScript bundle is approximately 205 kB gzipped, with the existing Vite size warning still present.

Validation: `make check` passed 76 PostgreSQL tests, 31 Python tests, and 15 Chromium tests, plus formatting, Clippy, and production build. Browser coverage now checks a rejected approval, blocked duplicate submission while a response is held, confirmed success, and retained review retry behavior. A separate real-app check verified dot expansion, stable width, keyboard focus, light/dark themes, reduced motion, 320-pixel layout, touch behavior, and navigation. The macOS desktop debug bundle rebuilt. Screenshots distinguish fixture approvals from actual workspace navigation; Windows/Edge and native desktop interactions remain unverified for this change.

Card interaction polish, September 14: shared web/desktop cards now use fine illuminated borders, a soft surface gradient, and blue hover/focus rims. Smaller navigation cards lift two pixels. Buttons add a brief sheen, hover lift, press feedback, and directional arrow movement. The treatment also reaches automatic-work decisions, investigation review, case detail, and memory cards. PaceUI remains the only template. Effects use CSS without new dependencies or JavaScript loops; reduced motion and forced-colors mode retain static accessible feedback.

Validation: `make check` passed all 76 PostgreSQL, 31 Python integration, and 15 Chromium tests, formatting, Clippy, and the production build. The macOS desktop debug bundle was rebuilt. A separate real-app browser check exercised hover rims, stable card dimensions, keyboard focus, reduced motion, 320-pixel layout, forced colors, and the evidence recipe. Light/dark, Connections, and mobile screenshots were inspected. No browser errors were recorded. Windows/Edge and native desktop interaction were not exercised for this visual change.

WebMCP and terminal tools, September 14: Overview now explains where to follow automatic work, review evidence, and find project memory. Connections includes project-scoped Codex setup, detected browser tool support, and a runnable evidence recipe. The browser and Python stdio adapters share three read-only tools for workspace status, bounded work lists, and exact-revision evidence. They use the existing local API. No task-direction form, new template, terminal process controller, or coding runtime was added. See [setup and transport limits](../integrations/relay-tools/README.md) and [RoboRev, terminal architecture, and cost research](research/terminal-harness-and-context-cost.md).

Validation: `make check` passed 76 PostgreSQL tests, six context-harness tests, 19 Plow tests, six terminal-tool tests, and 15 Chromium tests, plus formatting, Clippy, and production build. `make desktop-build` rebuilt the macOS debug application. A final input-validation refinement passed TypeScript, all six terminal tests, and all four focused browser tests. Browser fixtures exercise current and legacy registration, guest exclusion, argument bounds, the runnable recipe, and 320-pixel layout. Screenshots were inspected. Windows/Edge and native desktop interaction were not exercised for this change.

Real local verification: Chromium 145 registered all three tools with WebMCP enabled; its default configuration reported unsupported. The manual recipe read a real repository audit through three tool calls and opened that audit's review URL without JavaScript errors. The actual stdio server also returned real audit data. The ignored project Codex configuration was created, but the CLI did not load it in the current session. A new trusted repository session is required; model consumption remains unverified. These tools cannot approve, edit source, change policy, or send messages. The next execution milestone remains returning findings into ongoing coding work and measuring cost per successful task. No LLM token savings are claimed.

Autonomous work, September 14: the user rejected prompt-driven setup and per-change direction entry. The existing Sessions route now opens automatic audit history, evidence, approve/decline decisions, and pause/resume monitoring. New session, first-prompt, continuation, and direction forms were removed; earlier saved notes remain archived. The [local context harness](../integrations/context-harness/README.md) starts with `make dev`, watches tracked agent instructions every fifteen seconds, deduplicates snapshots, and automatically creates proposals for repeated whole-file content. Read-only evaluations run automatically through a versioned claim/result contract with bounded leases, recovery limits, stale-snapshot rejection, and pause fencing.

Validation: `make check` passed 76 PostgreSQL tests, six context-harness tests, 19 Plow adapter tests, 11 Chromium tests, formatting, Clippy, and production build. The browser suite proves that evaluation completes before candidate approval, checks lost-decision recovery and API disconnection, and verifies 320/390-pixel layouts. `make desktop-build` rebuilt the macOS debug application. The real local audit and light/dark/mobile views were inspected through Chromium; the in-app browser connection previously failed to initialize. Windows/Edge and native desktop interaction are not verified for this slice.

The real repository's first automatic audit inspected five tracked instruction files totaling 6,105 bytes and found no duplicate whole-file content. No proposal was invented for that audit. A temporary Git repository exercises discovery, automatic evaluation, approval, and retained decline decisions through the actual Python harness and browser. The evaluator measures lossless storage representation and exact source reconstruction only. It does not edit source, execute Hermes, select task-specific context, or establish token/cost savings. The broader coding harness, protected model evaluation, voice, and system-service installation remain open.

Earlier session implementation, superseded by Autonomous work: Sessions originally opened searchable, project-filtered history alongside a saved conversation and recorded case work. PostgreSQL stores first prompts, append-only local notes, and version-bound continuation links independently of bug cases. Previews, resuming, log download, mobile panel switching, and retry recovery after reload are implemented. The selected context-quality/token-cost objective has a real saved local session. Notes are explicitly unsent; no conversation adapter, reference collector, or continuous experiment engine was enabled. [RoboRev](research/roborev-workflow-benchmark.md) is recorded as a workflow benchmark, with no imported source or hooks.

Validation: `make check` passed 70 PostgreSQL tests, 19 Plow adapter tests, ten Chromium tests, formatting, Clippy, and the production web build. After session layout and error-message refinements, all three focused session browser tests passed, including the additional linked-validation test. Screenshots cover light/dark desktop and mobile; checks cover 320/390-pixel widths, continuation, a lost response followed by reload/retry, and recorded context provenance. `make desktop-build` rebuilt the macOS debug app. The in-app browser plugin could not initialize, so the real local app was inspected through standalone Chromium after that failure. Windows/Edge, native desktop interactions, and live Hermes/voice execution remain unverified for this slice.

Continuous improvement direction, September 14: [the proposed workflow](CONTINUOUS-IMPROVEMENT-PROPOSAL.md) expands the product discussion to persistent repository missions, relevant reference capture, isolated experiments, measured LLM/code improvements, and voice steering. The user selected context quality and reduced token cost as the first mission and requested conversation beside work, with searchable session history. The session implementation is described above. Watchers, mission scheduling, live voice, autonomous merge policy, and a new runtime remain proposed.

Plow Latch activation, September 14: the installed Mac app confirmed phone verification and completed its setup flow. Its main window reports Connected, zero agents, and zero MCP clients. Optional Google connection and Full Disk Access were skipped; usage sharing was turned off. This establishes the Mac session only. Relay still has no line credential or bridge configuration, and its live runner probe reports unavailable. No investigation, phone import, or owner delivery has run through Plow. The next acceptance case must connect a dedicated investigator, capture an actual approved Latch action, and show its evidence in Relay before any delivery is claimed.

Plow integration, September 13: the [host-local bridge](../integrations/plow/README.md) checks the credential's granted owner chat, imports a selected original phone report, and sends an exact approved outbox message. Durable receipts support recovery after provider acceptance without sending again. New intake can require the reviewed project policy version, checked in the same transaction as case creation and automatic queue admission. Existing message receipts retain their replay identity.

Validation: `make check` passed 66 PostgreSQL tests, eight Chromium tests, formatting, Clippy, the production web build, and the initial 18 bridge tests. After adding the complete report-length boundary, the focused bridge suite passed all 19 tests. Provider behavior is fixture-tested. New-line setup was started with Plow's official CLI and remains pending phone ownership activation; no live line grant, phone import, outbound delivery, or Latch action has been verified. No external message was sent by this work. The adapter does not manage a persistent connection or start a Hermes runtime. SwiftUI remains an offline simulation.

SwiftUI iPhone implementation, September 13: `apple/` contains an independent Xcode project, a shared Swift package, and native UI tests. [The iPhone guide](../apple/README.md) covers running the app. The first N-01 slice supports a text-driven simulated investigator call, editable report confirmation, a saved local example case, revision-bound repair approval or decline, inspectable context/observation/patch/check fixtures, stopping work, and a blocked-access scenario. Ending the call does not stop the investigation. The local fixture resumes after app suspension/relaunch; it is not a background execution service.

This is an isolated simulation with no network requests, microphone capture, real Hermes work, backend case writes, or owner delivery. SwiftUI shares domain concepts with web/Tauri but does not yet synchronize their data. The existing clients and backend source remain unchanged. Authenticated pairing, a conversation adapter, live voice, and a native macOS target remain later gates. The separate iPhone CI workflow is configured but has not run remotely.

Native validation: Xcode 26.4.1 built the iOS simulator app; seven Swift core tests and four native UI tests passed on iPhone 17 Pro / iOS 26.4. A focused rerun passed after moving explanatory text out of the pinned call controls at the largest accessibility text size. Screenshots cover Home, call, decisions, patch evidence, blocked access, and large text; light and dark Home screens were inspected. The UI suite verifies approval, end-call/stop separation, local case recovery after relaunch, and unavailable target behavior. `make check` also passed 65 PostgreSQL tests, eight Chromium tests, formatting, Clippy, and the production web build. Physical iPhone, iOS 17 runtime, VoiceOver operation, remote CI, and live integrations remain unverified.

SwiftUI planning, September 12: [Native client and investigator call](SWIFTUI-CLIENT-PLAN.md) specifies an iPhone-first simulated conversation, shared macOS client structure, case/evidence views, tool approvals, audio interruption handling, and authenticated device access. Gates N-01 through N-06 separate simulation, connection, live investigation, repair, and delivery. This is documentation only; no native client, voice adapter, pairing service, or provider transport was added.

Backend workflow implementation, September 12: migrations 0005–0009 add the durable evidence journal, text artifacts, typed findings/reviews, reviewed finding memory, versioned project policy, automatic investigation queue, scoped decisions, local intake sources, delivery attempts, and approved repair/verification contracts. [Backend workflow](BACKEND-WORKFLOW.md) documents the routes, state transitions and capability boundaries. Local validation imports and the shared evidence inspector let development results appear in the application with their actual provenance. This implements backend contracts across RP-02 through RP-14; it does not establish live provider integration or full release acceptance.

Execution remains opt-in and local-maintainer-only. Automatic investigation uses the existing single Hermes slot. Repair and verification dispatch require advertised isolated-execution/protected-test capabilities; the API never executes supplied shell commands itself. Provider delivery remains a local adapter protocol with no transport connected. Binary screenshot storage, signed provider intake, physical device testing, hard spending limits, and publication of repair-result memory remain open. Reported passing checks are distinct from independent verification.

Backend validation: `make check` passed formatting, Clippy with warnings denied, 65 PostgreSQL integration tests, the production frontend build and eight Chromium browser tests. Tests include repair finding context, equivalent commit hashes, final inspection context bounds, protected capability rejection, inherited memory revocation, 135-event cursor history, intake rollback, and uncertainty recovery. The evidence inspector tests pagination, local-inspector attribution, log focus/closing and withheld revoked content. `make desktop-build` rebuilt the macOS debug application. This is local and protocol-fixture validation; Windows CI, provider delivery, and live Hermes/Latch execution have not been exercised.

Investigation review implementation, September 12: Agent controls now has a shared web/desktop workspace with case selection, saved Hermes proposals, coordinator history, human evidence, review decisions, and the exact next-run context. Reviews are append-only and locally attributed. A requested correction can seed a bounded follow-up whose context digest is checked at admission. Original outputs, observations, and published memory are preserved. Source changes, superseded feedback, and revoked memory in earlier runs block stale follow-ups. This is partial delivery of RP-04/RP-05/RP-07/RP-08, not completion of the full protocol.

Validation: Rust formatting and Clippy passed, as did 28 isolated PostgreSQL tests and seven Chromium browser tests after fixes. The correction fixture covers retries, exact preview matching, history retention, mobile follow-up, usage, and cancellation. The compatibility fixture covers 25 wrapping case titles, both themes at 320 pixels, modal focus restoration, and sidebar close/focus behavior. Desktop and phone-width screenshots were inspected. The production frontend and macOS desktop bundle rebuilt. The local API was refreshed and the preview, run-history, and review routes returned 200 against the existing development cases. Hermes remains visibly unconfigured; no live agent work was claimed. Existing Vite large-chunk warnings remain.

Microsoft testing means Edge on Windows. The new Windows CI workflow covers the mocked investigation UI in Edge and native desktop compilation. It has not run in this local Mac session. The desktop now includes the required Windows ICO resource generated from its existing app icon. Windows installation, native interactions, and live Hermes execution remain unverified.

Configuration benchmark: the user selected Vercel. [The settings contract](research/vercel-configuration-benchmark.md) defines eight acceptance requirements for scoped configuration, clear effective values, checked connections, safe writes and change effects. PaceUI remains the template. Current process-managed runtime settings must not be presented as editable until a server-side configuration contract exists.

Delivery protocol, September 12: the accepted assessment is mapped to [15 implementation areas, resource requirements, dashboard contracts and acceptance gates](DELIVERY-PROTOCOL.md). The [Orca/Warp benchmark](research/orca-warp-dashboard-benchmark.md) uses official documentation; the intended Orca identity is provisional. [Domain terms](../CONTEXT.md) distinguish findings, reviewed evidence, replay, retest, approvals and delivery. These are planning artifacts; live integrations and automation are not newly implemented.

Product assessment, September 12: [Product depth review and skill map](PRODUCT-DEPTH-REVIEW.md) records the accessibility/user-assistance backlog, evidence and memory separation, phone/channel requirements, proposed automatic workflow, and acceptance gates. Three subagent audits and a read-only Chromium check informed it. This is planning documentation, not implemented automation. A key integration constraint is that raw agent receipts must not use the human-observation endpoint: it would misattribute evidence and invalidate the active run's source revision.

High-refresh grid: Perspective Grid no longer has a 20 fps cap. It renders every display animation callback, allowing 120/144+ Hz where the browser and hardware support it. Elapsed-time motion preserves the configured speed. Reduced-motion, hidden-page, offscreen, and context-loss pauses remain. ASCII Waves keeps its separate cap.

Frame scheduling validation: an instrumented software-WebGL browser recorded 31 grid draws for 31 display callbacks over 1.2 seconds, confirming the grid no longer skips display callbacks. Reduced motion stopped further draws. This validates scheduling, not physical 120 Hz performance; the software test environment was slower than a high-refresh display.

Grid color alignment: Perspective Grid now shares ASCII Waves' blue and opacity tokens, shader color conversion, and sidebar background. The bottom fade uses the sidebar background too. Both light and dark themes update live. Grid geometry and motion parameters remain unchanged.

Color validation: software-WebGL screenshots and computed styles confirmed matching background and blue values in light and dark mode, including a live theme switch with reduced motion enabled. `make check` passed all 24 existing database/browser tests, and the macOS desktop app rebuilt with the shared palette.

Perspective Grid replaced Rubber Fluid on the shared Connections page on September 12. The authenticated `@reactbits-starter/perspective-grid-tw` component uses the user's exact eight visual props, with height set to fill the configuration background. The purple grid shader and defaults remain intact. Local renderer changes add reduced motion, visibility suspension, display-driven frame updates, ResizeObserver sizing, context-loss handling, and cleanup on navigation. Rubber Fluid source and its blue background were removed; ASCII Waves remains in the sidebar.

Validation: `make check` passed 18 PostgreSQL tests and six browser tests. Additional software-WebGL checks verified animated frames, static reduced motion, context loss, navigation cleanup, and no overflow at 320 or 390 pixels. Light/dark and mobile screenshots were inspected. The production frontend and macOS desktop bundle rebuilt. Native GPU rendering remains untested. Perspective Grid shares Three.js with ASCII Waves and adds approximately 2.5 kB gzipped of component code; the existing large-chunk build warnings remain nonblocking.

Sidebar background implemented: authenticated React Bits Pro ASCII Waves now renders behind the shared web/desktop sidebar, including the mobile drawer. The original shader uses a direct Three.js adapter to avoid Fiber's React 19.3 peer conflict. It has theme-aware contrast, static reduced motion and WebGL fallback, a 20 fps cap, hidden-view suspension, and cleanup on unmount.

ASCII Waves validation, September 12: `make check` passed all 18 PostgreSQL and six browser tests. Additional Chromium checks with software WebGL verified changing animation frames, stable reduced-motion frames, context-loss fallback, navigation, and renderer removal when the sidebar closes. Forced WebGL unavailability retained a static decoration and usable navigation. Light, dark, and mobile-drawer screenshots were inspected. The final context-availability check passed TypeScript and the desktop production frontend build; `make desktop-build` rebuilt the macOS debug bundle. Native GPU rendering has not been separately exercised. Both registry credentials are ignored and absent from source and built assets. The lazy-loaded Three.js/effect chunk adds approximately 132 kB gzipped; Vite reports its standard large-chunk warning.

UI replacement implemented: the authenticated `@paceui/ultimate-dashboard-template` now supplies the shared web/Tauri dashboard. Its real source is adapted to cases, investigation controls, reviewed memory, and handoffs. See [the migration record](PACEUI-MIGRATION.md).

PaceUI validation, September 12: `make check` passed formatting, Clippy, 18 isolated PostgreSQL tests, the production web build, and six Chromium tests. Coverage includes the new dashboard, mobile sidebar, URL persistence, case workflow, guest isolation, themes, reduced motion, startup recovery, and the dedicated Agent controls lifecycle against the protocol fixture. Light/dark overview and mobile dashboard/case screenshots were inspected. `make desktop-build` produced `target/debug/bundle/macos/Repro Relay.app`. Final copy, contrast, and unused-import cleanup passed the production build and TypeScript unused-local check. The old unused landing preview was removed. No live Hermes/Latch or native save-dialog interaction was exercised.

The chart is loaded separately from the initial application bundle. Vite still reports a size warning for the approximately 502 kB initial JavaScript chunk, 162 kB gzipped. This does not block the build. Registry credentials are ignored, owner-readable only, and absent from source and bundled output. PaceUI retains its product license; no public redistribution was performed.

Updated September 12, 2026. Web and Tauri now share durable investigation controls through a Hermes HTTP adapter, validated with protocol fixtures. The hosted guest beta remains separate from the local runner. Public deployment is pending Railway account sign-in. Research and roadmap documents describe the larger hackathon product.

Implemented:

- Rust Axum API, Tokio, PostgreSQL migrations, durable cases and original observation history.
- React, TypeScript, Vite, Tailwind, PaceUI Ultimate Dashboard, shadcn controls, and the existing Radix tabs/event timeline. Overview counts and activity charts use workspace records. Neutral light/dark themes, a responsive sidebar drawer, and dedicated case and agent views replace the prior custom dashboard. Motion respects the device setting.
- New report, human-recorded observations, reviewed project memory, scoped exact-term retrieval, revocation, and Markdown exports.
- Deterministic investigator, repair, verifier, and update context views with source event IDs.
- Persisted handoff snapshots bound to case revision, current build, worker assignment, and referenced memories. The server rejects outdated snapshots and records the reason.
- Build updates preserve old observations and invalidate published memory. Concurrent writes use revision checks.
- Tauri desktop shell with native packet export and external browser links. The desktop client uses the same React case view as the web app; API and PostgreSQL run separately and must already be available. Client roles and the shared event contract are in [Client architecture](./CLIENT-ARCHITECTURE.md).
- Local Hermes adapter with capability checks, idempotent admission, one active investigation, frozen source context, durable remote IDs, restart polling, cooperative stop, and uncertain-dispatch reconciliation. Case changes and revoked source memory cancel queued work or request a remote stop. Agent answers remain proposals and do not publish evidence or memory. See [setup, limits, and recovery](./HERMES-RUNNER.md).
- Shared Agent context controls for start, stop, connection checks, reconciliation, saved run history, and reported token/cost usage. Case URLs retain selection across reload; Tauri can open the same case in the local browser.
- Desktop startup connection checks with setup commands and retry; native Workspace menu commands for new reports, case search, and connections; persisted window size, position, and maximized state. The API and PostgreSQL remain separate processes.
- Shared shadcn/ui Tooltip and Kbd components, CLI configuration, keyboard commands, and explicit pending labels. GSAP introduces panel and run-state changes with 220ms transitions. Reduced motion disables these effects and reverses an active transition when the device setting changes. Report dialogs focus their first field.
- Hosted guest workspaces with opaque HttpOnly cookies, scoped database access, seven-day expiry, capacity limits, and usefulness feedback. The template welcome screen opens a guest workspace with a labeled sample report.
- One deployment container serving the built React app and Rust API, Railway configuration, PostgreSQL TLS support, and graceful shutdown.

The handoff freshness check remains a local preflight and does not execute a repair or prove evidence contents. Investigation admission is a separate operation that reserves the local run slot and records source context. External actions cannot be rolled back if that context changes during dispatch. Existing handoffs and source evidence survive service restarts.

Not implemented or exercised live: Latch browser investigation and tool receipts, Plow phone intake, owner delivery, autonomous repair, independent patch verification, Mem0, runtime instruction evolution, persistent team accounts, signed installers, or App Store submission. Hermes HTTP behavior is fixture-tested; a live runtime still needs verification. There is no hard token/dollar budget or packaged backend sidecar. Unknown expired submissions retain the active slot pending operator investigation.

PostgreSQL stores each case aggregate as JSONB and the memory publication index relationally. A transaction-level advisory lock per workspace serializes mutations and cross-case memory validation within that workspace; separate workspaces can progress independently. SQL filters scope every case and memory operation to the server-derived workspace. Evidence links are not fetched automatically.

The earlier Python/SQLite prototype has been replaced. No SQLite migration is provided because it was an unfinished scaffold. Existing local SQLite files, if any, are left untouched and are not read by this build.

Next: follow [the next release plan](./ROADMAP.md). The two-day target is the hackathon build and deployment window, with the additional two days discussed reserved for operations and reliability. Exercise the adapter against the intended Hermes profile with one approved Latch action, persist its real receipt, add correction-aware conclusions and authorized owner delivery, then deploy and request verification. Phone access supports support/operations intake, clarification, approvals, and results. Desktop supports evidence review and developer work.

Validation commands and the repeatable browser flow are in README.md. Automated fixtures are separate from usage and from the local demo workspace.

Validation on September 12: the previous guest-beta milestone passed container health, static UI, guest isolation, CSRF, and feedback checks. Current runner verification is recorded below. Native save-dialog interaction has not been exercised; the available desktop accessibility bridge previously denied access.

Runner validation: `make check` passed Rust formatting, Clippy with warnings denied, 18 isolated PostgreSQL integration tests, the production frontend build, and four Chromium tests. These cover the existing case/guest flows and the fixture investigation lifecycle, reload, usage display, cancellation, and mobile width. Runner desktop-width and mobile screenshots were visually inspected. `make desktop-build` produced the macOS debug app with the shared run controls. Live Hermes/Latch execution and a native run-control interaction remain untested.

Desktop and motion validation: `make check` passed all 18 PostgreSQL integration tests and six Chromium tests, including desktop startup/reconnection with a simulated environment marker, keyboard commands, dialog focus, tooltips, and reduced motion. The native crate passed Clippy and the macOS debug bundle rebuilt. Light and dark startup screenshots were inspected. Browser tests do not exercise native menu delivery, OS window restoration, or the native save dialog; those still need an interactive desktop check.
