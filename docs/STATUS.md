# Current milestone

SwiftUI iPhone implementation, September 13: `apple/` contains an independent Xcode project, a shared Swift package, and native UI tests. [The iPhone guide](../apple/README.md) covers running the app. The first N-01 slice supports a text-driven simulated investigator call, editable report confirmation, a saved local example case, revision-bound repair approval or decline, inspectable context/observation/patch/check fixtures, stopping work, and a blocked-access scenario. Ending the call does not stop the investigation. The local fixture resumes after app suspension/relaunch; it is not a background execution service.

This is an isolated simulation with no network requests, microphone capture, real Hermes work, backend case writes, or owner delivery. SwiftUI shares domain concepts with web/Tauri but does not yet synchronize their data. The existing clients and backend source remain unchanged. Authenticated pairing, a conversation adapter, live voice, and a native macOS target remain later gates. The separate iPhone CI workflow is configured but has not run remotely.

Native validation: Xcode 26.4.1 built the iOS simulator app; seven Swift core tests and four native UI tests passed on iPhone 17 Pro / iOS 26.4. A focused rerun passed after moving explanatory text out of the pinned call controls at the largest accessibility text size. Screenshots cover Home, call, decisions, patch evidence, blocked access, and large text; light and dark Home screens were inspected. The UI suite verifies approval, end-call/stop separation, local case recovery after relaunch, and unavailable target behavior. `make check` also passed 65 PostgreSQL tests, eight Chromium tests, formatting, Clippy, and the production web build. Physical iPhone, iOS 17 runtime, VoiceOver operation, remote CI, and live integrations remain unverified.

SwiftUI planning, September 12: [Native client and investigator call](SWIFTUI-CLIENT-PLAN.md) specifies an iPhone-first simulated conversation, shared macOS client structure, case/evidence views, tool approvals, audio interruption handling, and authenticated device access. Gates N-01 through N-06 separate simulation, connection, live investigation, repair, and delivery. This is documentation only; no native client, voice adapter, pairing service, or provider transport was added.

Backend workflow implementation, September 12: migrations 0005–0009 add the durable evidence journal, text artifacts, typed findings/reviews, reviewed finding memory, versioned project policy, automatic investigation queue, scoped decisions, local intake sources, delivery attempts, and approved repair/verification contracts. [Backend workflow](BACKEND-WORKFLOW.md) documents the routes, state transitions and capability boundaries. Local validation imports and the shared evidence inspector let development results appear in the application with their actual provenance. This implements backend contracts across RP-02 through RP-14; it does not establish live provider integration or full release acceptance.

Execution remains opt-in and local-maintainer-only. Automatic investigation uses the existing single Hermes slot. Repair and verification dispatch require advertised isolated-execution/protected-test capabilities; the API never executes supplied shell commands itself. Provider delivery remains a local adapter protocol with no transport connected. Binary screenshot storage, signed provider intake, physical device testing, hard spending limits, and publication of repair-result memory remain open. Reported passing checks are distinct from independent verification.

Backend validation: `make check` passed formatting, Clippy with warnings denied, 65 PostgreSQL integration tests, the production frontend build and eight Chromium browser tests. Tests include repair finding context, equivalent commit hashes, final inspection context bounds, protected capability rejection, inherited memory revocation, 135-event cursor history, intake rollback, and uncertainty recovery. The evidence inspector tests pagination, local-inspector attribution, log focus/closing and withheld revoked content. `make desktop-build` rebuilt the macOS debug application. This is local and protocol-fixture validation; Windows CI, provider delivery, and live Hermes/Latch execution have not been exercised.

Investigation review implementation, September 12: Agent controls now has a shared web/desktop workspace with case selection, saved Hermes proposals, coordinator history, human evidence, review decisions, and the exact next-run context. Reviews are append-only and locally attributed. A requested correction can seed a bounded follow-up whose context digest is checked at admission. Original outputs, observations, and published memory are preserved. Source changes, superseded feedback, and revoked memory in earlier runs block stale follow-ups. This is partial delivery of RP-04/RP-05/RP-07/RP-08, not completion of the full protocol.

Validation: Rust formatting and Clippy passed, as did 28 isolated PostgreSQL tests and seven Chromium browser tests after fixes. The correction fixture covers retries, exact preview matching, history retention, mobile follow-up, usage, and cancellation. The compatibility fixture covers 25 wrapping case titles, both themes at 320 pixels, modal focus restoration, and sidebar close/focus behavior. Desktop and phone-width screenshots were inspected. The production frontend and macOS desktop bundle rebuilt. The local API was refreshed and the preview, run-history, and review routes returned 200 against the existing development cases. Hermes remains visibly unconfigured; no live agent work was claimed. Existing Vite large-chunk warnings remain.

Microsoft testing means Edge on Windows. The new Windows CI workflow covers the mocked investigation UI in Edge and native desktop compilation. It has not run in this local Mac session. The desktop now includes the required Windows ICO resource generated from its existing app icon. Windows installation, native interactions, and live Hermes execution remain unverified.

Configuration benchmark: the user selected Vercel. [The settings contract](research/vercel-configuration-benchmark.md) defines eight acceptance requirements for scoped configuration, clear effective values, checked connections, safe writes and change effects. PaceUI remains the template. Current process-managed runtime settings must not be presented as editable until a server-side configuration contract exists.

Delivery protocol, September 12: the accepted assessment is mapped to [15 implementation areas, resource requirements, dashboard contracts and acceptance gates](DELIVERY-PROTOCOL.md). The [Orca/Warp benchmark](research/orca-warp-dashboard-benchmark.md) uses official documentation; the intended Orca identity is provisional. [Domain terms](../CONTEXT.md) distinguish findings, reviewed evidence, replay, retest, approvals and delivery. These are planning artifacts; live integrations and automation are not newly implemented.

Product assessment, September 12: [Product depth review and skill map](PRODUCT-DEPTH-REVIEW.md) records the accessibility/user-assistance backlog, evidence and memory separation, phone/channel requirements, proposed automatic workflow, and acceptance gates. Three subagent audits and a read-only Chromium check informed it. This is planning documentation, not implemented automation. A key integration constraint is that raw agent receipts must not use the human-observation endpoint: it would misattribute evidence and invalidate the active run's source revision.

High-refresh grid: Perspective Grid no longer has a 20 fps cap. It renders every display animation callback, allowing 120/144+ Hz where the browser and hardware support it. Elapsed-time motion preserves the configured speed. Reduced-motion, hidden-page, offscreen, and context-loss pauses remain. ASCII Waves keeps its separate cap.

Frame scheduling validation: an instrumented software-WebGL browser recorded 31 grid draws for 31 display callbacks over 1.2 seconds, confirming the grid no longer skips display callbacks. Reduced motion stopped further draws. This validates scheduling, not physical 120 Hz performance; the software test environment was slower than a high-refresh display.

Grid color alignment: Perspective Grid now shares ASCII Waves' blue and opacity tokens, shader color conversion, and sidebar background. The bottom fade uses the sidebar background too. Both light and dark themes update live. Grid geometry and motion parameters remain unchanged.

Color validation: software-WebGL screenshots and computed styles confirmed matching background and blue values in light and dark mode, including a live theme switch with reduced motion enabled. `make check` passed all 24 existing database/browser tests, and the macOS desktop app rebuilt with the shared palette.

Perspective Grid replaced Rubber Fluid on the shared Connections page on September 12. The authenticated `@reactbits-starter/perspective-grid-tw` component uses the user's exact eight visual props, with height set to fill the configuration background. The purple grid shader and defaults remain intact. Local renderer changes add reduced motion, visibility suspension, display-driven frame updates, ResizeObserver sizing, context-loss handling, and cleanup on navigation. Rubber Fluid source and its blue background were removed; ASCII Waves remains in the sidebar.

Validation: `make check` passed 18 PostgreSQL tests and six browser tests. Additional software-WebGL checks verified animated frames, static reduced motion, context loss, navigation cleanup, and no overflow at 320 or 390 pixels. Light/dark and mobile screenshots were inspected. The production frontend and macOS desktop bundle rebuilt. Native GPU rendering remains untested. Perspective Grid shares Three.js with ASCII Waves and adds approximately 2.5 kB gzipped of component code; the existing large-chunk build warnings remain nonblocking.

Sidebar background implemented: authenticated React Bits Pro ASCII Waves now renders behind the shared web/desktop sidebar, including the mobile drawer. The original shader uses a direct Three.js adapter to avoid Fiber's React 19.3 peer conflict. It has theme-aware contrast, static reduced motion and WebGL fallback, a 20 fps cap, hidden-view suspension, and cleanup on unmount.

ASCII Waves validation, September 12: `make check` passed all 18 PostgreSQL and six browser tests. Additional Chromium checks with software WebGL verified changing animation frames, stable reduced-motion frames, context-loss fallback, navigation, and renderer removal when the sidebar closes. Forced WebGL unavailability retained a static decoration and usable navigation. Light, dark, and mobile-drawer screenshots were inspected. The final context-availability check passed TypeScript and the desktop production frontend build; `make desktop-build` rebuilt the macOS debug bundle. Native GPU rendering has not been separately exercised. Both registry credentials are ignored and absent from source and built assets. The lazy-loaded Three.js/effect chunk adds approximately 132 kB gzipped; Vite reports its standard large-chunk warning.

UI replacement implemented: the authenticated `@paceui/ultimate-dashboard-template` now supplies the shared web/Tauri dashboard. Its real source is adapted to cases, investigation controls, reviewed memory, and handoffs. See [the migration record](PACEUI-MIGRATION.md).

PaceUI validation, September 12: `make check` passed formatting, Clippy, 18 isolated PostgreSQL tests, the production web build, and six Chromium tests. Coverage includes the new dashboard, mobile sidebar, URL persistence, case workflow, guest isolation, themes, reduced motion, startup recovery, and the dedicated Agent controls lifecycle against the protocol fixture. Light/dark overview and mobile dashboard/case screenshots were inspected. `make desktop-build` produced `target/debug/bundle/macos/Repro Relay.app`. Final copy, contrast, and unused-import cleanup passed the production build and TypeScript unused-local check. The old unused landing preview was removed. No live Hermes/Latch or native save-dialog interaction was exercised.

The chart is loaded separately from the initial application bundle. Vite still reports a size warning for the approximately 502 kB initial JavaScript chunk, 162 kB gzipped. This does not block the build. Registry credentials are ignored, owner-readable only, and absent from source and bundled output. PaceUI retains its product license; no public redistribution was performed.

Updated September 12, 2026. Web and Tauri now share durable investigation controls through a Hermes HTTP adapter, validated with protocol fixtures. The hosted guest beta remains separate from the local runner. Public deployment is pending Railway account sign-in. Research and roadmap documents describe the larger hackathon product.

Implemented:

- Rust Axum API, Tokio, PostgreSQL migrations, durable cases and original observation history.
- React, TypeScript, Vite, Tailwind, PaceUI Ultimate Dashboard, shadcn controls, and the existing Radix tabs/event timeline. Overview counts and activity charts use workspace records. Neutral light/dark themes, a responsive sidebar drawer, and dedicated case and agent views replace the prior custom dashboard. Motion respects the device setting.
- New report, human-recorded observations, reviewed project memory, scoped exact-term retrieval, revocation, and Markdown exports.
- Deterministic investigator, repair, verifier, and update context views with source event IDs.
- Persisted handoff snapshots bound to case revision, current build, worker assignment, and referenced memories. The server rejects outdated snapshots and records the reason.
- Build updates preserve old observations and invalidate published memory. Concurrent writes use revision checks.
- Tauri desktop shell with native packet export and external browser links. The desktop client uses the same React case view as the web app; API and PostgreSQL run separately and must already be available. Client roles and the shared event contract are in [Client architecture](./CLIENT-ARCHITECTURE.md).
- Local Hermes adapter with capability checks, idempotent admission, one active investigation, frozen source context, durable remote IDs, restart polling, cooperative stop, and uncertain-dispatch reconciliation. Case changes and revoked source memory cancel queued work or request a remote stop. Agent answers remain proposals and do not publish evidence or memory. See [setup, limits, and recovery](./HERMES-RUNNER.md).
- Shared Agent context controls for start, stop, connection checks, reconciliation, saved run history, and reported token/cost usage. Case URLs retain selection across reload; Tauri can open the same case in the local browser.
- Desktop startup connection checks with setup commands and retry; native Workspace menu commands for new reports, case search, and connections; persisted window size, position, and maximized state. The API and PostgreSQL remain separate processes.
- Shared shadcn/ui Tooltip and Kbd components, CLI configuration, keyboard commands, and explicit pending labels. GSAP introduces panel and run-state changes with 220ms transitions. Reduced motion disables these effects and reverses an active transition when the device setting changes. Report dialogs focus their first field.
- Hosted guest workspaces with opaque HttpOnly cookies, scoped database access, seven-day expiry, capacity limits, and usefulness feedback. The template welcome screen opens a guest workspace with a labeled sample report.
- One deployment container serving the built React app and Rust API, Railway configuration, PostgreSQL TLS support, and graceful shutdown.

The handoff freshness check remains a local preflight and does not execute a repair or prove evidence contents. Investigation admission is a separate operation that reserves the local run slot and records source context. External actions cannot be rolled back if that context changes during dispatch. Existing handoffs and source evidence survive service restarts.

Not implemented or exercised live: Latch browser investigation and tool receipts, Plow phone intake, owner delivery, autonomous repair, independent patch verification, Mem0, runtime instruction evolution, persistent team accounts, signed installers, or App Store submission. Hermes HTTP behavior is fixture-tested; a live runtime still needs verification. There is no hard token/dollar budget or packaged backend sidecar. Unknown expired submissions retain the active slot pending operator investigation.

PostgreSQL stores each case aggregate as JSONB and the memory publication index relationally. A transaction-level advisory lock per workspace serializes mutations and cross-case memory validation within that workspace; separate workspaces can progress independently. SQL filters scope every case and memory operation to the server-derived workspace. Evidence links are not fetched automatically.

The earlier Python/SQLite prototype has been replaced. No SQLite migration is provided because it was an unfinished scaffold. Existing local SQLite files, if any, are left untouched and are not read by this build.

Next: follow [the next release plan](./ROADMAP.md). The two-day target is the hackathon build and deployment window, with the additional two days discussed reserved for operations and reliability. Exercise the adapter against the intended Hermes profile with one approved Latch action, persist its real receipt, add correction-aware conclusions and authorized owner delivery, then deploy and request verification. Phone access supports support/operations intake, clarification, approvals, and results. Desktop supports evidence review and developer work.

Validation commands and the repeatable browser flow are in README.md. Automated fixtures are separate from usage and from the local demo workspace.

Validation on September 12: the previous guest-beta milestone passed container health, static UI, guest isolation, CSRF, and feedback checks. Current runner verification is recorded below. Native save-dialog interaction has not been exercised; the available desktop accessibility bridge previously denied access.

Runner validation: `make check` passed Rust formatting, Clippy with warnings denied, 18 isolated PostgreSQL integration tests, the production frontend build, and four Chromium tests. These cover the existing case/guest flows and the fixture investigation lifecycle, reload, usage display, cancellation, and mobile width. Runner desktop-width and mobile screenshots were visually inspected. `make desktop-build` produced the macOS debug app with the shared run controls. Live Hermes/Latch execution and a native run-control interaction remain untested.

Desktop and motion validation: `make check` passed all 18 PostgreSQL integration tests and six Chromium tests, including desktop startup/reconnection with a simulated environment marker, keyboard commands, dialog focus, tooltips, and reduced motion. The native crate passed Clippy and the macOS debug bundle rebuilt. Light and dark startup screenshots were inspected. Browser tests do not exercise native menu delivery, OS window restoration, or the native save dialog; those still need an interactive desktop check.
