[package]
name = "relay-desktop"
version = "0.2.0"
edition = "2024"
license = "MIT"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

rfd = "0.15"
tauri-plugin-opener = "2"
